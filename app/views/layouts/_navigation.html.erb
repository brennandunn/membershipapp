<% if ENV['FG'] %>

<% else %>
<ul class="nav">
  <% if user_signed_in? %>
    <% unless ENV['FG'] %>
    <li class="course">
    	<%= link_to 'Course', course_modules_path %>
    </li>
    <% end %>
    <li class="discussion">
    	<%= link_to 'Forum', ENV['DISCOURSE'] %>
    </li>
    <% unless ENV['FG'] %>
    <li class="resources">
    	<%= link_to 'Resources', resource_path %>
    </li>
    <% end %>
    <li class="edit_account">
    	<%= link_to 'Edit account', edit_user_registration_path, :class => "edit_account_link" %>
    	<%= link_to 'Logout', destroy_user_session_path, :method=>'delete', :class => "logout" %> 
    </li>
    <% if current_user.has_role? :admin %>
      <li>
      <%= link_to 'Admin', admin_users_path %>
      </li>
    <% end %>

  <% end %>
</ul>
<% end %>